# Run 2026-09-11 (repair) — site-wide soft-404

## Measured
- **GSC** (user-supplied manual export, 28 days, 2026-08-13 → 2026-09-09): 7 clicks, 1,409 impressions, 0.5% CTR, avg position 26.19. **0 branded impressions** — all visibility is non-brand.
- **Coverage**: 1 indexed, 6 not indexed (5 Page with redirect, 1 Not found 404).
- **health_diff** (`.seo/health/2026-09-12.json`, baseline): 1 page; one broken internal link, target `/cdn-cgi/l/email-protection` (404).
- **Production probes**: `http://`→`https://` 301, `www.`→apex 301, `/index.html`→`/` 308, `/gutter-guards.html`→`/gutter-guards` 308. `/services` and `/about` return **200 with the homepage HTML** — the soft-404.
- **truth_check**: 6 rules, 0 contradictions, 0 missing.
- **repo_changes**: 0 commits touched watched paths.

## Vitals
| Vital | Reading |
|---|---|
| Speed | median TTFB 175 ms; 0 pages over 1000 ms |
| Relevance | homepage is the only page with impressions; avg pos 26; 0 branded demand |
| Click rate | site CTR 0.50%; impression-weighted gap 0.99× |
| Links | 1 broken, 0 redirecting, 1 orphan |

## Candidate chosen
`repair` — site-wide soft-404 (score 22). A defect on the only page that already ranks, and site-wide, so it outranks any new page (technical lane priority tier 1: crawlability & indexation). User confirmed the repair over creating the installation service page and refreshing the homepage.

## Did
1. Added `404.html` at the repo root. Cloudflare Pages serves a root `404.html` with **HTTP 404** for unmatched paths, so unknown URLs stop returning the homepage. It is a design-matched page with `meta robots noindex,follow`, exactly one `<h1>`, and links home + `/gutter-guards` + the phone number.
2. Wrapped the contact `mailto:` in `<!--email_off-->` in `index.html`, `gutter-guards.html`, and `404.html`. Cloudflare's Email Address Obfuscation was rewriting it to `/cdn-cgi/l/email-protection#…`, which is the 1 GSC "Not found (404)". The per-block directive stops the rewrite.
3. Investigated the 5 "Page with redirect": all are benign, correct, single-hop canonicalization. No fix.

## Verified
- Local: HTML tag-balance clean on `404.html`, `index.html`, `gutter-guards.html`; exactly one `<h1>` each; all three now carry `<!--email_off-->`; `404.html` serves 200 at `/404.html` on the local server.
- Redirect probes: single hop each, no chains or loops.
- **Unverified until deploy**: production 404 status on unknown paths; `health_diff.py` clean on the fixed rule; that `/llms.txt` now 404s instead of serving the homepage. Filed as NY-9.

## Not done (next run's debt)
- Cloudflare-managed `robots.txt` still blocks ClaudeBot/Amazonbot (dashboard only) — NY-8.
- The installation service page (the biggest demand cluster, ~400 impressions of install/replacement queries at pos 21–30) — not built.
- A real `llms.txt` (AEO lane). It will now 404 honestly rather than serve homepage HTML.
- Re-run `health_diff.py` against a deployed fingerprint.
- The 5 redirect URLs are still unnamed (the GSC export reports counts only); they appear benign from probes.

## Needs you
NY-9 (post-deploy verification + optional zone-wide email-obfuscation toggle), NY-8 (AI retrieval policy), plus NY-2…NY-7 unchanged.

## Spend
$0 (manual research; no paid APIs connected).
