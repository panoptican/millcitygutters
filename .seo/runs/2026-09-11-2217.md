# SEO Run — 2026-09-11 22:17

- Worktree: `/Users/jason/Projects/websites/millcitygutters`, branch `main`
- Mode: interactive (foundation + first content run)
- `config.git.mode`: `none`
- Run id: `2026-09-11-2217`

## Measured

Manual-research mode. Tools available: `python3`, `git`, `websearch`/`webfetch`. Not connected: Search Console, DataForSEO, Google Analytics 4, OpenSEO, Bing Webmaster, PageSpeed Insights, server logs, headless browser, community-search skill, social-posting skill.

Panels:

- **Truth check** (`truth_check.py`, 6 rules): no contradictions, no missing claims.
- **Site health diff** (`health_diff.py`): first-run baseline written to `.seo/health/2026-09-12.json`; 1 page. One violation: `[broken-internal-link]` `https://millcitygutters.com/cdn-cgi/l/email-protection` (404) — Cloudflare email-obfuscation artifact, low severity.
- **Repo changes** (`repo_changes.py`): 0 commits touched watched paths.
- **robots check** (`robots_check.py`): FAIL — Cloudflare-managed `robots.txt` blocks Amazonbot, Applebot-Extended, Bytespider, CCBot, ClaudeBot, CloudflareBrowserRenderingCrawler, Google-Extended, GPTBot, meta-externalagent. Retrieval agents ClaudeBot/Amazonbot blocked. Content-Signal: search=yes, ai-train=no, use=reference. (Its "llms.txt present" line is a false positive: `/llms.txt` serves homepage HTML.)
- **Depth check**: 1 page, depth 0, no orphans.
- **Tech audit**: no findings (needs a full URL for `--domain`).
- **Crawl check** homepage: 200, 398 words, score 8/14; weakest passages Timely 3/8, Quality 4/8, Transparent Costs 5/8 (open with "We", no specifics).
- **Census / brand split**: SKIPPED (require a GSC input; not connected). **Outcomes**: 0 rows, priors empty.
- **Live site facts**: homepage TTFB 167 ms, 12288 bytes, Cloudflare. **Soft-404**: every unknown path returns HTTP 200 with the full homepage (e.g. `/random-404-test-xyz` → 200 `text/html`). No `_redirects`, `_headers`, `404.html`, or real `llms.txt` in the repo.
- **Demand evidence** (websearch): r/TwinCities gutter-price and mesh-vs-covers threads (2024–2026), r/Rochester 2026 ice-buildup thread, r/minnesota gutter-guard advice. Competitors already ranking on the exact decision intent: mplsroofing.com (Owl Roofing) 2026 guard and cost pages; wgccinc.com (Weatherguard) "Are Gutter Guards Worth It in Minnesota?" and "Do Gutter Guards Cause Ice Dams in Minnesota?".

## Candidates

| # | Action | Target | Movement | Conf | Strategic | Fresh | Effort | Score | Demand evidence |
|---|---|---|---|---|---|---|---|---|---|
| 1 | create-editorial | Are gutter guards worth it in Minneapolis (decision guide) | 5 | 4 | 5 | 5 | 4 | **34** | r/TwinCities + r/Rochester threads; 2 competitors rank; no local-installer owner |
| 2 | create-editorial | Seamless gutter cost in Minneapolis | 5 | 3 | 4 | 4 | 3 | 27 | r/TwinCities price threads; Owl Roofing cost page |
| 3 | repair | Site-wide soft-404 (unknown paths 200 homepage) | 3 | 5 | 3 | 3 | 4 | 22 | Live 404 probe |
| 4 | measure-only | — | — | — | — | — | — | <bar | cold start, no GSC |

No override tier fired (no reachability/index regression, no truth contradiction, no user-facing harm).

## Chose

`create-editorial` — **"Are Gutter Guards Worth It in Minneapolis?"** at `/gutter-guards` (`gutter-guards.html`).

Why it won: demand is proven by dated local threads and two competitors already ranking on the exact decision intent, yet no Twin Cities installer owns it; the guide can state a concrete, local information gain (a three-variable decision rule plus candid "what guards don't solve") and it feeds the site's biggest gap — it is the second page on a one-page site.

Interactive: user selected this candidate at the checkpoint.

## Did

- Created `gutter-guards.html` at repo root (2,473 prose words; Cloudflare Pages serves it at `/gutter-guards`).
  - Decision-intent structure: answer-first 60-word lead, TOC, a 5-row guard-type comparison table, two hand-authored inline SVG figures (decision flow; ice-dam cross-section) using repo tokens (`#d76540`, `#1d1d1f`), worth-it / not-worth-it checklists, ice-dam explanation, "what we do not solve", install approach, 7-question FAQ.
  - JSON-LD `@graph`: `Article` + `BreadcrumbList` + `FAQPage`.
  - Self-canonical, og/twitter tags, one `<h1>` (header brand demoted to `.site-title` so the article title is the sole H1).
- Edited `index.html`: added 2 inbound links to `/gutter-guards` (services intro; Gutter Protection Systems card).
- Edited `sitemap.xml`: added `/gutter-guards` with `lastmod 2026-09-11`.
- Wrote brief `.seo/briefs/gutter-guards.md`.
- Registered ledger rows, performance row, link inventory + anchor variants, needs-you NY-8.

**Gates**

- `word_count.py gutter-guards.html --min 1200` → 2,473 ✓
- `truth_check.py` → 0 contradictions, 0 missing ✓
- Exactly one `<h1>` per page (index 1, gutter-guards 1) ✓
- Forbidden-word grep (seamlessly/revolutionary/best-in-class/cheap/one-stop shop/first-party price patterns) → clean ✓
- HTML tag balance (html.parser) → no errors, no unclosed tags, both files ✓
- JSON-LD parses; title 55 chars (≤60), meta description 145 chars (≤155) ✓
- In-body links to homepage: `/#services` ×3, `/#costs` ×2, `/#footer` ×2, `/` ×2 ✓
- Inbound links ≥2 from the crawl hub (`index.html`): ×2 ✓
- No noindex; body copy is server-rendered static HTML; page will return 200 ✓
- **Unchecked:** live rendered check and `tech_audit.py --schema <url>` — page is not deployed yet, and no headless browser is available. SVG figures were authored but **not visually verified** (no browser). Visual check of the two SVG figures in light/dark must be done after deploy.

## Not done

1. **Soft-404 repair deferred** (candidate 3). Add `404.html` (or `_redirects`) so unknown paths return a real 404 and `/llms.txt` stops serving the homepage. Not done because the create action outranked it and one piece per run; it is now #2 backlog.
2. **SVG figures not visually verified** — no browser tool.
3. **Schema not validated against a live URL** — page not deployed.
4. **No GSC/DFS measurement** — the performance rows are `unmeasured`; can't yet say whether the page indexes or ranks.
5. **Homepage crawl-check weaknesses** (Timely 3/8, Quality 4/8, Transparent Costs 5/8) noted, not acted on — future `refresh`.
6. **Cloudflare robots.txt / AI retrieval blocks** — needs dashboard access; NY-8.
7. **`compact_state.py`** not run — only 1 health file, not first-of-month.

## Needs you

- NY-8 opened this run (Cloudflare AI retrieval-agent policy, in full in the final message).
- NY-1..NY-7 remain open (restated in the final message).

## Spend

$0. No paid calls. Manual research via websearch only. No DataForSEO connected, so nothing skipped for budget.

## Portfolio

Not a census run. (Invisible share / create_gate not computed — requires GSC.)
